zojax.superlance
----------------

zojax.superlance is a package of plugin utilities for monitoring and controlling
processes that run under supervisor. Currently, it provides httpok and memmon
plugins with small customisations for running plugins functionality only after
defined port becomes being listened by some application. In case no port is
defined both plugins run basic functionality.

httpokport
----------

memmonport
----------
